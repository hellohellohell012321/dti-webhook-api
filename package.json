{
  "name": "vercel-webhook-api",
  "version": "1.0.0",
  "description": "A simple Vercel API to send filtered embeds to a Discord webhook with rate limiting.",
  "main": "api/send.js",
  "type": "module",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "engines": {
    "node": "18.x"
  }
}
